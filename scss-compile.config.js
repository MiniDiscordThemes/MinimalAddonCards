/** @type {import('bd-scss/lib/config').Config} */
export default {
    meta: {
        name: "MinimalAddonCards" /* The official name of your theme. */,
        scss: "cards" /* The name of your base scss file. */,
        repo: "MinimalAddonCards" /* The name of your theme repository. */,
        version: "dev" /* The version of your theme. */
    }
};
